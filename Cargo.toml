[package]
name = "chip8-emulator"
version = "0.1.0"
edition = "2021"
description = "CHIP-8 emulator written in Rust."
license = "MIT"

[features]
default = []
window = ["dep:minifb"]

[dependencies]
minifb = { version = "0.27", optional = true }
